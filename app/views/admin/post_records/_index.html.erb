<table class="table text-center">
  <thead>
    <tr>
      <th>投稿日</th>
      <th>投稿者名</th>
      <th>部位</th>
      <th>トレーニング名</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% post_records.each do |post_record| %>
    <tr>
      <td>
        <%= post_record.created_at.strftime('%Y/%m/%d') %>
      </td>
      <td>
        <%= post_record.user.name %>
      </td>
      <td>
        <%= post_record.category.name %>
      </td>
      <td>
      <%= link_to admin_post_record_path(post_record.id) do %>
        <%= post_record.training_name %>
      <% end %>
      </td>
    </tr>
      <% end %>
  </tbody>
</table>
